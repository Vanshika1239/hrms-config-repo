server:
  port: 8082

spring:
  application:
    name: EMPLOYEE-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/employee_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true
